# clubs

## structure

- `club-backend/` — REST API
- `club-web/` — Next.js frontend

## getting started

cd club-backend && cp .env.example .env && npm install && npm run dev
cd club-web && cp .env.example .env.local && npm install && npm run dev
